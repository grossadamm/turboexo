# macOS
.DS_Store

# Claude session data
.claude/

# TunerStudio auto-backups (milestone tunes are tracked; git is the history now)
restorePoints/

# TunerStudio UI / session artifacts (large, non-diffable)
TuneView/
dashboard/

# TunerStudio session state & editor backups
projectCfg/persistedChannelValues.properties*
*.bak
*.bkup
